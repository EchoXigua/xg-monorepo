{
  "name": "test-demo",
  "private": true,
  "version": "1.2.11",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "deploy": "vue-tsc && vite build",
    "deploy:github": "vue-tsc && vite build --mode github",
    "preview": "vite preview",
    "clear:modules": "rimraf node_modules"
  },
  "devDependencies": {
    "vue-tsc": "^2.0.19"
  }
}
